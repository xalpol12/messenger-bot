export interface ImageFormData {
  name?: string,
  customUri?: string,
}

export interface ImageInfo {
  id: string,
  name: string,
  url: string,
  type: string,
  size: number,
  createdAt: string,
}
